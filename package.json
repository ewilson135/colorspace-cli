{
  "name": "colorspace-cli",
  "version": "0.1.0",
  "description": "Convert colors between hex, rgb, and hsl from the command line",
  "type": "module",
  "main": "./dist/color.js",
  "bin": {
    "colorspace": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
